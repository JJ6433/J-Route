<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.NoticeMapper">

    <select id="findAll" resultType="NoticeDto">
        SELECT notice_id AS noticeId, title, content, is_active AS active, 
               created_at AS createdAt, updated_at AS updatedAt
        FROM notices
        ORDER BY created_at DESC
    </select>

    <select id="findActive" resultType="NoticeDto">
        SELECT notice_id AS noticeId, title, content, is_active AS active, 
               created_at AS createdAt, updated_at AS updatedAt
        FROM notices
        WHERE is_active = TRUE
        ORDER BY created_at DESC
    </select>

    <select id="findById" resultType="NoticeDto">
        SELECT notice_id AS noticeId, title, content, is_active AS active, 
               created_at AS createdAt, updated_at AS updatedAt
        FROM notices
        WHERE notice_id = #{noticeId}
    </select>

    <insert id="insert" parameterType="NoticeDto" useGeneratedKeys="true" keyProperty="noticeId">
        INSERT INTO notices (title, content, is_active)
        VALUES (#{title}, #{content}, #{active})
    </insert>

    <update id="update" parameterType="NoticeDto">
        UPDATE notices
        SET title = #{title},
            content = #{content},
            is_active = #{active}
        WHERE notice_id = #{noticeId}
    </update>

    <delete id="delete">
        DELETE FROM notices WHERE notice_id = #{noticeId}
    </delete>

</mapper>
